[package]
name = "reformat-plugins"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Plugin system for reformat transformers"
readme = "../README.md"

# No dependencies yet: this crate is a placeholder for a future plugin API.
[dependencies]
